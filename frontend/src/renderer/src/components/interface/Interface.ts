export interface searchResultInterface{
    id: string;
    title: string;
    duration: string;
    thumbnail: string;
    channel : string;
}

export interface audioInfo{
    url:string;
    title: string;
    duration: string;
    format: string;
    quality: string;
    thumbnail: string;
}